// Q for a given array of numbers , print the square  of each value using the forEach loop

const numbers = [1, 2, 3, 4, 5];

numbers.forEach(num => {
    console.log(num*num)
});


// Q we are given the array of marks of students . filter out the marks of students that scored 90